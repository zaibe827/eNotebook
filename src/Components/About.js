// import React, { useContext, useEffect } from 'react'
// import noteContext from '../Context/notes/noteContext'

const About = () => {
  
  // useEffect(() => {
  //   a.update();
  //   // eslint-disable-next-line
  // }, [])
  return (
    <div>
      {/* Using the context */}
      {/* This is about {a.name} and belong to class {a.class} */}

      {/* changing name and class after 1 second */}
      {/* This is about {a.state.name} and belong to class {a.state.class} */}
    </div>
  )
}

export default About
