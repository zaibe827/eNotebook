import { createContext } from "react";

const noteContext = createContext(); //syntax for creating new context
//this context will hold all the states related to the notes


export default noteContext;
